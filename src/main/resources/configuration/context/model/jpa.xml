<?xml version="1.0" encoding="UTF-8"?>
<beans
        xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:util="http://www.springframework.org/schema/util"
        xmlns:tx="http://www.springframework.org/schema/tx"
        xsi:schemaLocation="
             http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
             http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd
             http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

    <!-- Entity manager factory -->

    <bean id="jpaVendorAdapter" class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
        <property name="databasePlatform" value="${app.hibernate.dialect.class}"/>
    </bean>

    <bean id="jpaDialect" class="org.springframework.orm.jpa.vendor.HibernateJpaDialect"/>

    <bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="persistenceUnitName" value="ApplicationDataSource"/>
        <property name="persistenceXmlLocation" value="classpath:/configuration/jpa/persistence.xml"/>
        <property name="jpaVendorAdapter" ref="jpaVendorAdapter"/>
        <property name="jpaProperties">
            <props>
                <prop key="hibernate.show_sql">${app.hibernate.show_sql}</prop>
                <prop key="hibernate.format_sql">${app.hibernate.format_sql}</prop>
                <prop key="hibernate.use_sql_comments">${app.hibernate.use_sql_comments}</prop>
                <prop key="hibernate.order_inserts">${app.hibernate.order_inserts}</prop>
                <prop key="hibernate.order_updates">${app.hibernate.order_updates}</prop>
                <prop key="hibernate.cache.use_second_level_cache">${app.hibernate.cache.use_second_level_cache}</prop>
                <prop key="hibernate.cache.use_query_cache">${app.hibernate.cache.use_query_cache}</prop>
                <prop key="hibernate.hbm2ddl.auto">${app.hibernate.hbm2ddl.auto}</prop>
                <prop key="hibernate.id.new_generator_mappings">${app.hibernate.id.new_generator_mappings}</prop>
            </props>
        </property>
   </bean>

    <!-- Transaction manager -->

    <bean name="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactory"/>
        <property name="jpaDialect" ref="jpaDialect"/>
        <property name="rollbackOnCommitFailure" value="true"/>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager" mode="proxy"/>

</beans>